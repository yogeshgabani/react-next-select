[build]
  base = "."
  command = "npm install && npm run build && cd examples && npm install --legacy-peer-deps && npm run build"
  publish = "examples/.next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
